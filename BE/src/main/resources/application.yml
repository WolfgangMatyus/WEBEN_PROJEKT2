spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rsystem?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
server:
  port: 8181
logging:
  level:
    org:
      springframework:
        security: DEBUG
